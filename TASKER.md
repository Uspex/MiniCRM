# TASKER.md

## Текущая задача

### История изменений операций + отчёт для выявления накрутки коэффициента (2026-06-22)

**Задача:** Сохранять историю правок операции (кто и что изменил: было → стало). Показывать историю на странице редактирования операции. Добавить тип отчёта «История изменений операций», чтобы root мог выявлять пользователей, которые накручивают коэффициент (правят `product_count`, `runtime`, `activity_id` и т.п. после создания).

**Решения:** период отчёта — по дате самой правки (`task_histories.created_at`); история накапливается только с момента внедрения; фильтры отчёта применяются к операции-владельцу.

| # | Шаг | Статус |
|---|------|--------|
| 1 | Миграция `task_histories` (task_id, editor_id, changes JSON, created_at) | ✅ |
| 2 | Модель `TaskHistory` (fillable, cast changes→array, editor/task; `UPDATED_AT=null`) | ✅ |
| 3 | Запись истории в `TaskController@update` (isDirty по значимым полям) | ✅ |
| 4 | Блок истории на странице редактирования операции | ✅ |
| 5 | Локализация ru/en — `task.history.*`, `report.type.operations_history`, заголовки CSV | ✅ |
| 6 | Новый тип отчёта `operations_history` (Report константа + getTypes; форма/бейдж — автоматически) | ✅ |
| 7 | Генератор `ReportService::generateOperationsHistory` + `formatHistoryValue` | ✅ |

Миграция применена, `view:clear` выполнен, все PHP-файлы проходят `php -l`.

### Фильтр подразделения на странице Операции (2026-05-13)

**Задача:** Добавить select-фильтр «Подразделение» на страницу `admin.task.index` (одиночный, в стиле существующих фильтров). Доступен только при наличии пермишена `task_all_users` (как фильтр сотрудников). Фильтрация через `users.department`.

| # | Шаг | Статус |
|---|------|--------|
| 1 | Локализация — ключ `task.search.department` в ru/en | ✅ |
| 2 | `TaskController@index` — `$allDepartments` + фильтрация через `User::where('department', ...)` | ✅ |
| 3 | Blade — select `department` + добавить в `request()->hasAny([...])` | ✅ |
| 4 | Очистить кеш вьюшек | ✅ |

### Поиск в фильтре «Сотрудник» на странице Операции (2026-05-13)

**Задача:** Превратить одиночный select сотрудников в Select2 с поиском (аналогично дашборду).

| # | Шаг | Статус |
|---|------|--------|
| 1 | Blade — `id="filterUserId"`, пустой первый option | ✅ |
| 2 | JS — инициализация Select2 (placeholder + allowClear) | ✅ |
| 3 | Очистить кеш вьюшек | ✅ |

### Откат среднего → коэффициент «Итого» = сумма факта / сумма плана (2026-05-04)

**Задача:** Вернуть в столбце «Итого» (дашборд: строки активностей и футер; CSV-отчёт coefficient: последний столбец) расчёт `round(сумма_факта / сумма_плана, 2)` вместо среднего арифметического дневных коэффициентов. Заголовок — «Итого».

| # | Шаг | Статус |
|---|------|--------|
| 1 | Локализация — удалить ключи `dashboard.table_average` и `report.csv.average` (ru/en) | ✅ |
| 2 | Дашборд: строка активности — `factTotal/planTotal` | ✅ |
| 3 | Дашборд: футер — `grandFact/grandPlan` | ✅ |
| 4 | CSV-отчёт coefficient — `totalFact/totalPlan` в последнем столбце | ✅ |
| 5 | Очистить кеш вьюшек | ✅ |

### Активности без plan_time: план = факт (2026-05-04)

**Задача:** Активности без `plan_time` (нерегулярные — уборка и т.п.) перестали ломать «Итого» и коэффициент. Решение: для них план приравнивается к факту, ячейка показывает `N/N`, коэф 1.0 — нейтрально.

| # | Шаг | Статус |
|---|------|--------|
| 1 | `DashboardController` — для активностей без `plan_time` plan-датасет = fact-датасет | ✅ |
| 2 | `ReportService::generateCoefficient` — для активностей без `plan_time` `$plan += $fact` | ✅ |
| 3 | Очистить кеш вьюшек | ✅ |

### Переносимый пакет блока «Обновление сервера» (2026-04-22)

**Задача:** Собрать в `docs/portable/server-update/` переносимый пакет страницы `/admin/server` (бэкап БД → git pull → migrate → seed → cache, список бэкапов со скачиванием) с инструкцией для переноса в другой проект.

| # | Шаг | Статус |
|---|-----|--------|
| 1 | Создать структуру `docs/portable/server-update/` | ✅ |
| 2 | Скопировать 4 самостоятельных файла (ServerController, index.blade, lang/ru, lang/en) | ✅ |
| 3 | Сниппет маршрутов `_snippets/routes.php` | ✅ |
| 4 | Сниппет пункта меню `_snippets/sidebar-menu.blade.php` | ✅ |
| 5 | Сниппет строк локализации `_snippets/lang-common.md` | ✅ |
| 6 | README.md — установка, зависимости, адаптация, smoke-тест | ✅ |

### Поле `work_day` + редактирование смены/даты + рефакторинг аналитики

**Задача:** Добавить поле `work_day` (date) в Task — рабочий день по смене. Если задача создана в 01:00 15.04 (2-я смена), `work_day` = 14.04. Возможность указать смену и `work_day` при создании/редактировании с предзаполнением. Сидер. Перевести аналитику и отчёты с `DATE(created_at - INTERVAL ...)` на `work_day`.

**План:**

| # | Шаг | Статус |
|---|------|--------|
| 1 | Миграция — `work_day` (date, nullable, index) | ✅ |
| 2 | Модель Task — `work_day` в fillable, `resolveShiftData()` возвращает `work_day` | ✅ |
| 3 | Локализация — ключи `task.form.fields.work_day`, `task.form.fields.shift` в ru/en | ✅ |
| 4 | Валидация — `work_day` и `shift` в TaskCreateRequest и TaskUpdateRequest | ✅ |
| 5 | Формы create/edit — поля `work_day` (date) и `shift` (select) с предзаполнением | ✅ |
| 6 | Контроллер store/update — обработка `work_day`, `shift`, пересчёт `work_start`/`work_finish` | ✅ |
| 7 | Сидер `TaskWorkDaySeeder` (chunkById) | ✅ |
| 8 | DashboardController — `work_day` вместо `DATE(created_at - INTERVAL ...)`, убрать dayStartHour-сдвиги | ✅ |
| 9 | ReportService — аналогичная замена на `work_day` | ✅ |

---

### Среднее значение вместо итога в CSV-отчёте

**Задача:** Последний столбец CSV — вместо суммарного коэффициента (`totalFact/totalPlan`) показывать среднее арифметическое дневных коэффициентов. Переименовать заголовок на «Среднее значение».

| # | Шаг | Статус |
|---|------|--------|
| 1 | Локализация — ключ для «Среднее значение» в ru/en | ✅ |
| 2 | `ReportService.php` — среднее дневных коэффициентов + новый заголовок | ✅ |

### Среднее значение в таблице дашборда

**Задача:** Аналогично CSV — последний столбец таблицы дашборда: переименовать «Итого» → «Среднее значение», коэффициент в тултипе = среднее дневных коэффициентов.

| # | Шаг | Статус |
|---|------|--------|
| 1 | Локализация — ключ `dashboard.table_average` в ru/en | ✅ |
| 2 | Blade — заголовок + среднее коэффициентов в строках и футере | ✅ |

### Исправление формата чисел в CSV-отчёте ✅

---

## Архив выполненных задач

### Типы отчётов: coefficient / productivity / operations (2026-04-17)

В таблицу `reports` добавлено поле `type` (string, default `coefficient`). В форме генерации — select с тремя типами. `ReportService` разбит на три метода-генератора через `match` по типу. Productivity — строки «Сотрудник × Тип работ», колонки-даты, «Итого». Operations — детальный список задач (created_at, work_day, смена (только название), сотрудник, подразделение, тип работ, кол-во, время, сообщение). Имя скачиваемого файла с суффиксом типа. Бейдж типа в списке отчётов.

| # | Шаг | Статус |
|---|------|--------|
| 1 | Миграция — `type` string default `'coefficient'`, index | ✅ |
| 2 | Модель `Report` — константы-строки, `type` в `$fillable`, `getTypes()` | ✅ |
| 3 | Локализация ru/en — `report.type.*`, `report.form.fields.type`, заголовки CSV | ✅ |
| 4 | `ReportController@generate` — валидация `type`, сохранение | ✅ |
| 5 | Вьюшка формы генерации — select типа | ✅ |
| 6 | Вьюшка списка отчётов — колонка/бейдж типа | ✅ |
| 7 | `ReportService` — диспетчер + три генератора | ✅ |
| 8 | Имя скачиваемого файла — суффикс типа | ✅ |

### Удаление редактирования даты создания задачи + пермишен task_edit_date (2026-04-14)

Убрано поле `created_at` из формы, удалён пермишен `task_edit_date` из модели, БД, валидации и контроллера.

| # | Шаг | Статус |
|---|------|--------|
| 1 | Blade — убрано поле | ✅ |
| 2 | Контроллер — убрана обработка | ✅ |
| 3 | Валидация — убрано правило | ✅ |
| 4 | Permission модель — убрана константа | ✅ |
| 5 | БД — удалён пермишен | ✅ |

### Фильтр по сменам + колонка «Смена» с тултипом (2026-04-14)

Select-фильтр по сменам на странице операций. Колонка «Смена» с тултипом `work_start – work_finish`. Локализация ru/en.

| # | Шаг | Статус |
|---|------|--------|
| 1 | Локализация | ✅ |
| 2 | Контроллер — `allShifts` + фильтрация | ✅ |
| 3 | Blade — фильтр + колонка с тултипом | ✅ |

### Заполнение work_start/work_finish/shift при создании задачи (2026-04-14)

Новый метод `Task::resolveShiftData(Carbon)` возвращает `[shift, work_start, work_finish]` на основе настроек смен. `TaskController@store` использует его для автозаполнения всех трёх полей. Обработка смен через полночь.

| # | Шаг | Статус |
|---|------|--------|
| 1 | Модель — `resolveShiftData()` | ✅ |
| 2 | Контроллер — автозаполнение при создании | ✅ |

### Фильтр по дате на странице Операции (2026-04-14)

Daterangepicker на странице `admin.task.index` — фильтрация по `created_at`. Локализация ru/en, контроллер с `whereBetween` + `withQueryString`, JS-инициализация с preset-диапазонами.

| # | Шаг | Статус |
|---|------|--------|
| 1 | Локализация — ключ `task.search.date` в ru/en | ✅ |
| 2 | Контроллер `TaskController@index` — фильтрация по `created_at` | ✅ |
| 3 | Blade `task/index.blade.php` — daterangepicker | ✅ |

### Колонка «Пользователь» в списке задач для task_all_users (2026-04-13)

В `resources/views/admin/task/index.blade.php` заголовок и ячейка колонки «Пользователь» проверяли `hasRole(ROLE_ROOT)` — пропуск при рефакторинге 2026-04-09. Заменено на `$canViewAll` (пермишен `task_all_users`). Кеш вьюшек очищен.

| # | Шаг | Статус |
|---|------|--------|
| 1 | Заголовок колонки — `$canViewAll` | ✅ |
| 2 | Ячейка строки — `$canViewAll` | ✅ |
| 3 | `php artisan view:clear` | ✅ |

### Улучшения отчётов + аудит локализации (2026-04-09)

Polling статусов отчётов каждые 5 сек (пока есть pending/processing), с динамическим обновлением бейджей и кнопок действий. Иконка фильтра с тултипом в списке отчётов. Аудит en-локализации: добавлен `lang/en/errors.php`, ключ `menu.dashboard` в `common.php`.

| # | Шаг | Статус |
|---|------|--------|
| 1 | Polling статусов (эндпоинт + JS) | ✅ |
| 2 | Иконка фильтров в списке | ✅ |
| 3 | Аудит en-локализации | ✅ |

### Пермишены «все пользователи» для каждой группы (2026-04-09)

Заменена проверка `hasRole('root')` на отдельные пермишены: `analytics_all_users`, `report_all_users`, `task_all_users`. Каждый в своей группе. Контроллеры (Dashboard, Report, Task) и вьюшки используют `$canViewAll = can(...)`. Кеш вьюшек очищен. Сидер выполнен.

| # | Шаг | Статус |
|---|------|--------|
| 1 | Permission модель — 3 константы | ✅ |
| 2 | Сидер | ✅ |
| 3 | DashboardController | ✅ |
| 4 | ReportController | ✅ |
| 5 | TaskController | ✅ |
| 6 | Вьюшки (dashboard, report/index, task/index) | ✅ |

### Фильтры для отчётов + только коэффициент в экспорте (2026-04-09)

Добавлены фильтры на страницу отчётов (сотрудники root-only, типы работ, смены, подразделения) — аналогично дашборду. Фильтры сохраняются в JSON-поле `filters` таблицы reports и применяются при генерации CSV. В CSV оставлен только коэффициент (число). Для не-root пользователей user_id жёстко ограничен текущим пользователем.

| # | Шаг | Статус |
|---|------|--------|
| 1 | Миграция — JSON-поле `filters` | ✅ |
| 2 | Модель Report — $fillable + $casts | ✅ |
| 3 | Контроллер — данные фильтров + валидация + сохранение | ✅ |
| 4 | Вьюшка — Select2-фильтры в форме генерации | ✅ |
| 5 | ReportService — фильтры + только коэффициент в CSV | ✅ |
| 6 | Локализация en — добавлены activities, shifts | ✅ |

### Просмотр логов в админке (2026-04-08)

Установлен `opcodesio/log-viewer` v3.24.0. Доступ ограничен `role:root` через `LogViewer::auth()` в AppServiceProvider. Пункт «Логи» в sidebar.

### Обработка ошибок генерации отчётов (2026-04-08)

Job при ошибке записывает лог в файл `report_error_{id}.txt` (текст ошибки + stack trace), сохраняет как file_path отчёта. На UI иконка скачивания лога для failed-отчётов. `failOnTimeout = true`.

### Перевод генерации отчётов на Job (2026-04-08)

Генерация CSV перенесена из синхронного вызова в `GenerateReportJob` (`$timeout=0`, `$tries=1`). ReportService переписан на chunk-обработку Task (по 500). Добавлен статус отчёта (pending/processing/completed/failed) с бейджами на UI.

| # | Шаг | Статус |
|---|------|--------|
| 1 | Миграция (status + file_path nullable) | ✅ |
| 2 | Модель (константы статусов) | ✅ |
| 3 | GenerateReportJob | ✅ |
| 4 | ReportService (chunk) | ✅ |
| 5 | Контроллер (dispatch) | ✅ |
| 6 | Вьюшка (бейджи статусов) | ✅ |
| 7 | Локализация ru/en | ✅ |

### Таблица коэффициентов по сотрудникам на дашборде (2026-04-07) — ОТКАТ

Откачено: второй запрос слишком нагружал систему (таймаут 30 сек на продакшне).

### Линия «План» на графике дашборда (2026-04-07)

Пунктирная линия плана для каждого типа работ (`plan_quantity`). Таблица: формат `факт/план`, цветной нижний бордер (зелёный — выполнен, жёлтый — нет). Легенда графика скрыта, цветные квадратики в таблице.

| # | Шаг | Статус | Заметка |
|---|------|--------|---------|
| 1 | Контроллер | ✅ | plan datasets + isPlan флаг |
| 2 | Chart.js | ✅ | borderDash пунктир, без точек, без легенды |
| 3 | Таблица | ✅ | факт/план, цветные бордеры, квадратики цветов |
| 4 | Локализация | ✅ | ru: «План: :name», en: «Plan: :name» |

### Модель «Настройки» (Setting) (2026-04-07)

Управление сменами и подразделениями из админки. Таблица settings (type unique, value JSON), CRUD, пермишены, рефакторинг смен и подразделений на данные из БД.

| # | Шаг | Статус | Заметка |
|---|------|--------|---------|
| 1 | Миграция + Модель | ✅ | Таблица settings (type unique, value JSON). Модель с константами и get/set |
| 2 | Сидер | ✅ | SettingSeeder: смены из конфига + пустые подразделения |
| 3 | Локализация | ✅ | setting.php ru/en + menu.setting в common |
| 4 | Контроллер + маршруты | ✅ | SettingController (index/edit/update) + routes |
| 5 | Вьюшки | ✅ | index + shifts/edit + departments/edit с динамическим добавлением строк |
| 6 | Пермишены | ✅ | GROUP_SETTING + PERMISSION_SETTING_EDIT, сидер выполнен |
| 7 | Рефакторинг смен | ✅ | Task::resolveShift + DashboardController → Setting::get |
| 8 | Рефакторинг подразделений | ✅ | user create/edit — select из Setting::get(departments) |
| 9 | Меню | ✅ | Пункт «Настройки» в sidebar, по пермишену setting_edit |

### Поля «План кол-во» и «План время» для Activity (2026-04-06)

Два новых поля в типе работ: `plan_quantity` (integer) и `plan_time` (decimal 8,2). Миграция, модель, формы, валидация, список.

| # | Шаг | Статус | Заметка |
|---|------|--------|---------|
| 1 | Миграция | ✅ | `plan_quantity` (uint), `plan_time` (decimal 8,2) after slug |
| 2 | Модель | ✅ | `plan_quantity`, `plan_time` в `$fillable` |
| 3 | Локализация | ✅ | ru/en — list.head, form.fields |
| 4 | Формы | ✅ | Оба поля в create и edit |
| 5 | Валидация | ✅ | nullable, integer/numeric, min:0 в обоих Request |
| 6 | Список | ✅ | Колонки в index + контроллер store/update обновлён |

### Поле «Подразделение» для пользователей (2026-04-03)

Nullable строковое поле `department` в таблице users. Формы create/edit, колонка в index, валидация.

| # | Шаг | Статус | Заметка |
|---|------|--------|---------|
| 1 | Миграция | ✅ | `department` nullable string after `username` |
| 2 | Модель | ✅ | `department` в `$fillable` |
| 3 | Локализация | ✅ | ru/en — list.head, form.fields |
| 4 | Формы | ✅ | Поле в create и edit |
| 5 | Валидация | ✅ | nullable, string, max:191 |
| 6 | Список | ✅ | Колонка в index, `tb-col-md` |

### Редактирование даты создания задачи + фикс мобильной вёрстки (2026-04-03)

Пермишен `task_edit_date` для редактирования `created_at` в форме задачи. Поле отображается всегда, редактируемо только при наличии пермишена. Убрано поле статуса. Фикс отступа кнопок фильтров на мобильных в index.

| # | Шаг | Статус | Заметка |
|---|------|--------|---------|
| 1 | Пермишен | ✅ | Константа `PERMISSION_TASK_EDIT_DATE` + `getPermissionGroupList()` |
| 2 | Сидер | ✅ | `PermissionSeeder` выполнен |
| 3 | Локализация | ✅ | `created_at` в ru и en |
| 4 | Форма редактирования | ✅ | `datetime-local`, `disabled readonly` без пермишена, убран статус, компактная вёрстка |
| 5 | Валидация | ✅ | `created_at` — nullable, date |
| 6 | Контроллер | ✅ | `created_at` игнорируется без пермишена |
| 7 | Фикс мобильной вёрстки | ✅ | `mt-3 mt-md-0` на кнопках фильтров в index |
